# Secur APP — Frontend

Expo Router + React Query + Axios + Zustand.

## Setup

1. Copie `.env.example` para `.env` e ajuste `EXPO_PUBLIC_API_URL`.
2. Instale deps:
   npm install
3. Rode:
   npx expo start -c

## Alias

- Configurado `@/` (raiz do `frontend/`).
